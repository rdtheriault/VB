Sub CheckDate()


Dim rng As Range, cell As Range
Dim msg As String

Set rng = Range("D2:AB611")

For Each cell In rng
  If Not (IsDate(cell.Value)) Then
    msg = msg + " " + cell.Address    
  End If
Next cell

MsgBox msg


End Sub

